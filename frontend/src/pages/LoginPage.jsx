import React from 'react'; // Importa a biblioteca React

function LoginPage() { // Define um componente funcional chamado LoginPage
  return ( // Retorna o JSX que representa a interface do componente
    <div>
      <h2>Página de Login</h2>
      <p>Este é o formulário de login.</p>
      {/* Futuramente, aqui virá o formulário real */}
    </div>
  );
}

export default LoginPage; // Exporta o componente para que possa ser usado em outros arquivos
